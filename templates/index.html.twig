{% extends 'base.html.twig' %}

{% block title %}
    Movies
{% endblock %}

{% block body %}
    <div>Results: {{ count }}</div>
    <ul>
        {% for key, movie in movies %}
            <li>{{ key+1 }}. {{ movie.title }}</li>
        {% endfor %}
    </ul>

{% endblock %}

